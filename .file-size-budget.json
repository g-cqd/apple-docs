{
  "max_lines": 400,
  "soft_target": 300,
  "_doc": "One file temporarily exceeds the 400-line ceiling. The check still hard-fails on growth past the recorded baseline so the file can't drift further.",
  "exempt": [
    {
      "path": "src/web/worker/search-worker.js",
      "lines": 411,
      "_reason": "Search Web Worker — title index, inverted/prefix builders, scoring, postMessage protocol, and the new origin-validating init guard all in a single file because Web Workers can't share scope across module boundaries without bundler support. Pinned to 411."
    }
  ]
}
